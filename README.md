# Build-Neural-Netowrk-for-Analyzing-the-Prices-of-Houses
Building a neural network using Numpy with activation function (ReLU) and adjusting the weights in such a way that the loss function is minimized. 
